# ── Geheime Schlüssel (NICHT committen!) ───────────────────────────────
# 1. Kopiere diese Datei und nenne die Kopie ".env"
# 2. Trage deinen 21st.dev Magic API-Key ein
# 3. Alternativ: MAGIC_API_KEY als Umgebungs-Secret in den
#    Projekteinstellungen (Claude Code on the web) setzen.
#
# Wird von .mcp.json (Magic MCP-Server) als API_KEY genutzt.

MAGIC_API_KEY=hier_deinen_neuen_key_eintragen
